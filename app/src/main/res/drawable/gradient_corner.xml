<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- MAIN HEADER -->
    <item>
        <shape android:shape="rectangle">

            <gradient
                android:startColor="#F7C62F"
                android:endColor="#F77A33"
                android:angle="270"/>

            <corners
                android:bottomLeftRadius="50dp"
                android:bottomRightRadius="50dp"/>

        </shape>
    </item>

    <item
        android:gravity="bottom|end"
        android:bottom="-30dp"
        android:right="95dp">

        <shape android:shape="oval">
            <size
                android:width="68dp"
                android:height="56dp"/>
            <solid android:color="@android:color/white"/>
        </shape>

    </item>

    <!-- BIGGER BITE -->
    <item
        android:gravity="bottom|end"
        android:bottom="-20dp"
        android:right="15dp">

        <shape android:shape="oval">
            <size
                android:width="102dp"
                android:height="75dp"/>
            <solid android:color="@android:color/white"/>
        </shape>

    </item>

    <!-- Remove the invalid CardView tag that was here -->

</layer-list>